{
  "name": "gulp-config",
  "version": "1.0.0",
  "description": "A gulp config for a front stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guillaume Duclos",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.0"
  }
}
